# Shopify App Configuration
# This file configures your Shopify app for native embedding

name = "ESL System"
client_id = "acd736c296901cbe912fadfc3127c01f"
# Shopify CLI will update this URL automatically when running 'shopify app dev'
# It creates a tunnel and updates the app configuration
application_url = "http://localhost:3000"
embedded = true

[access_scopes]
scopes = "read_products,write_products,read_inventory,write_inventory,read_orders,write_orders"

[auth]
# Note: Redirect URLs must be configured in Shopify Partner Dashboard
# This file is for local development only
redirect_urls = [
  # Add your backend callback URL here for local dev
  # Production callback URL should be set in Partner Dashboard
]

[webhooks]
api_version = "2026-01"

[pos]
embedded = true

